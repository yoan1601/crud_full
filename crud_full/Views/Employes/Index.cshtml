@model IEnumerable<crud_full.Models.Employe>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.datenaissance)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Departement)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.datenaissance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Departement.nomdept)
            </td>
                    
            <td>
                <a asp-action="Edit" asp-route-id="@item.idemploye">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idemploye">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idemploye">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
